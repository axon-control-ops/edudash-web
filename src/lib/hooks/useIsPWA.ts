/**
 * useIsPWA — Legacy hook stub.
 * PWA install mode has been removed. This returns false always.
 * Keep this file to avoid breaking any remaining imports.
 */
export function useIsPWA() {
  return { isPWA: false, isLoading: false };
}